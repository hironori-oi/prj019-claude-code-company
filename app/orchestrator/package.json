{
  "name": "@clawbridge/orchestrator",
  "version": "0.1.0",
  "private": true,
  "description": "Open Claw orchestrator skeleton (W0 stub)",
  "type": "module",
  "scripts": {
    "build": "echo 'orchestrator: W0 stub, no build'",
    "test": "echo 'orchestrator: W0 stub, no tests'",
    "lint": "echo 'orchestrator: W0 stub'",
    "typecheck": "echo 'orchestrator: W0 stub, no tsconfig yet'"
  },
  "dependencies": {
    "@clawbridge/harness": "workspace:*",
    "@clawbridge/claude-bridge": "workspace:*",
    "@clawbridge/openclaw-runtime": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.7.0",
    "vitest": "^2.1.0",
    "@types/node": "^22.10.0"
  }
}
