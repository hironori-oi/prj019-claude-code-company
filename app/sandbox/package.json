{
  "name": "@clawbridge/sandbox",
  "version": "0.1.0",
  "private": true,
  "description": "Vercel Sandbox / local sandbox layer (W0 stub)",
  "type": "module",
  "scripts": {
    "build": "echo 'sandbox: W0 stub, no build'",
    "test": "echo 'sandbox: W0 stub, no tests'",
    "lint": "echo 'sandbox: W0 stub'",
    "typecheck": "echo 'sandbox: W0 stub, no tsconfig yet'"
  },
  "devDependencies": {
    "typescript": "^5.7.0",
    "vitest": "^2.1.0",
    "@types/node": "^22.10.0"
  }
}
