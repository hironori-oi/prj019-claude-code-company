{
  "name": "@clawbridge/notify",
  "version": "0.1.0",
  "private": true,
  "description": "Notification dispatch layer (Slack webhook / Telegram / Email). Round 12 Dev-B: Slack webhook POST 配線 (DEC-019-057)。",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    },
    "./slack": {
      "types": "./dist/slack-webhook-sender.d.ts",
      "import": "./dist/slack-webhook-sender.js"
    }
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "vitest run",
    "lint": "echo 'notify: lint TBD'",
    "typecheck": "tsc --noEmit -p tsconfig.json"
  },
  "dependencies": {
    "zod": "^3.24.0"
  },
  "devDependencies": {
    "typescript": "^5.7.0",
    "vitest": "^2.1.0",
    "@types/node": "^22.10.0"
  }
}
