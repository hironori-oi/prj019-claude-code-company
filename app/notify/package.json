{
  "name": "@clawbridge/notify",
  "version": "0.1.0",
  "private": true,
  "description": "Notification dispatch layer (mail/Slack/Discord) (W0 stub)",
  "type": "module",
  "scripts": {
    "build": "echo 'notify: W0 stub, no build'",
    "test": "echo 'notify: W0 stub, no tests'",
    "lint": "echo 'notify: W0 stub'",
    "typecheck": "echo 'notify: W0 stub, no tsconfig yet'"
  },
  "devDependencies": {
    "typescript": "^5.7.0",
    "vitest": "^2.1.0",
    "@types/node": "^22.10.0"
  }
}
